spring.application.name=farmchainx-backend
server.port=8080

# ?? MySQL Database ??
spring.datasource.url=jdbc:mysql://localhost:3306/farmchainx
spring.datasource.username=root
spring.datasource.password=1234

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ?? Email (Gmail) for OTP ??
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=thotatejaswini22@gmail.com
spring.mail.password=YOUR_GMAIL_APP_PASSWORD
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ?? JWT ??
jwt.secret=farmchainx_secret_key_2024_minimum_256_bits
jwt.expiration.ms=86400000

# ?? File Upload ??
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB
```

---

## Step 3 ? Replace these two things:
```
YOUR_MYSQL_PASSWORD  ?  your actual MySQL password
YOUR_GMAIL_APP_PASSWORD  ?  we will set this up later